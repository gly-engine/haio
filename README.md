# Haio

> **Haio (Homebrew Asset IO)** is a conversion toolchain for images and audio, focused on homebrew environments and non-conventional asset formats used by legacy consoles and limited hardware. It can also operate as an image CDN, fetching and transforming assets directly from remote storage buckets.

```
cmake -Bbuild -H.
```

```
make -C build
```